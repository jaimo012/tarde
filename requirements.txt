# 클라우드타입 배포용 최적화된 requirements.txt
# Python 3.12 호환성 및 최소 의존성으로 구성

# 필수 HTTP 라이브러리
requests>=2.31.0

# DART API 및 구글 스프레드시트 연동
gspread>=6.0.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# 데이터 처리 (최소한만)
pandas>=2.1.0
numpy>=1.26.0

# 환경 설정 관리
python-dotenv>=1.0.0

# 로깅
loguru>=0.7.0

# 스케줄러 (클라우드타입 무한 실행용)
schedule>=1.2.0

# HTML/XML 파싱
beautifulsoup4>=4.12.0

# 날짜 처리
python-dateutil>=2.8.0
pytz>=2023.3

# 주식 데이터 조회 (한국거래소 공식 데이터)
pykrx>=1.0.40
setuptools>=65.0.0  # pykrx 의존성

# 차트 생성
matplotlib>=3.7.0
pillow>=10.0.0  # 이미지 처리