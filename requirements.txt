# 클라우드타입 배포용 최적화된 requirements.txt
# Python 3.12 호환성 및 최소 의존성으로 구성

# 필수 HTTP 라이브러리
requests>=2.31.0

# DART API 및 구글 스프레드시트 연동
gspread>=6.0.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# 데이터 처리 (최소한만)
pandas>=2.1.0
numpy>=1.26.0

# 환경 설정 관리
python-dotenv>=1.0.0

# 로깅
loguru>=0.7.0

# HTML/XML 파싱
beautifulsoup4>=4.12.0

# 날짜 처리
python-dateutil>=2.8.0
